# Database Configuration
# Get this from Neon: https://console.neon.tech
# Format: postgresql://[user]:[password]@[host]/[database]?sslmode=require
DATABASE_URL=your_neon_database_url_here

# Session Secret (generate with: openssl rand -base64 32)
# Only needed for production deployments with session management
SESSION_SECRET=your_random_secret_here

# Node Environment
# Leave unset for local development (enables mock auth)
# Set to 'production' on Render
# NODE_ENV=production

# Replit Auth (optional)
# Leave unset to use mock auth for local development
# Only set these if deploying to Replit
# REPL_ID=
# ISSUER_URL=
